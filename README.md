1. Суть проекта в одном предложении

Нейросервис, который читает новости и автоматически оценивает:
«Это новость, которая с высокой вероятностью двинет рынок (куда и по каким активам) или шум?»

Доступ к этому сервису — через Telegram Miniapp:
вставил ссылку / текст → получил анализ + сигналы.

2. Кому это нужно (целевые пользователи)

Частные инвесторы / трейдеры

Хотят быстро понять: новость реально важная или «расшифровка очевидного».

Фильтрация шума, подсказка по направлению движения.

Инвест- и финтех-каналы в Telegram

Могут встроить твой сервис в свой Miniapp или пользоваться сами,

делать «быструю аналитику» к новостям: «триггерный / не триггерный инфоповод».

Финансовые организации

Брокеры, управляющие, банки (инвест-блок),

хотят внутренний инструмент для аналитиков и маркетинга (push по горячим темам).

Создатели стратегий и роботов

Используют твой сервис как feature provider:
«на вход — новости, на выход — численный триггерный сигнал».

3. Что такое «инвестиционный триггер» в рамках модели

Для продукта нужно чётко определить, что мы считаем триггером. В модели это будет набор предсказываемых параметров:

Сила триггера (Trigger Score, 0–100):

0–30 — шум / фон

30–60 — слабый / средний триггер

60–100 — сильный триггер (есть смысл следить / действовать)

Направление влияния:

🚀 Bullish (ожидается рост),

🔻 Bearish (ожидается падение),

😐 Neutral / неопределённо.

Горизонт влияния:

очень краткосрочно (минуты–часы),

краткосрочно (1–3 дня),

среднесрок (до месяца).

Тип события (event type):

отчётность компании,

дивиденды,

санкции / геополитика,

регуляторика (ЦБ, правительство),

сделки M&A,

новости о банкротстве / дефолте,

макроэкономика (ставка, инфляция, незанятость),

техногенные / форс-мажорные события.

К каким активам относится:

конкретный тикер / эмитент,

сектор (нефть, IT, банки, металлурги),

рынок в целом (индекс).

4. Функционал проекта (уровень продукта)
4.1. Основной сценарий в Miniapp

Сценарий пользователя:

Заходит в Miniapp.

Вставляет:

ссылку на новость или

текст новости или

скрин (в будущем, с OCR).

Нажимает «Анализировать».

Получает:

оценку важности (0–100) и вербальное: «сильный триггер»,

направление: «скорее негативно для X»,

какие активы затрагивает: тикеры / сектора,

какой тип события: санкции, дивиденды, отчёт,

возможный горизонт реакции,

краткий пересказ и интерпретацию простым языком:
«С высокой вероятностью в течение 1–3 дней усилится давление на акции банковского сектора РФ».

Может сохранить новость в:

«Мои триггеры»

или отправить результат в чат/канал.

4.2. Дополнительные функции Miniapp

Лента «горячих триггеров»

Для выбранных тикеров / секторов / рынка,

Новости с высоким Trigger Score сортируются сверху,

Фильтры: по силе, по направлению, по типу события.

Подписка на уведомления

Пользователь задаёт:

тикеры / сектора,

минимальный порог Trigger Score (например, >70),

Miniapp через бот-уведомления присылает:

«Появился сильный негативный триггер по сектору нефти»,

с кратким описанием.

История и статистика по триггерам

Список всех сработавших триггеров,

Краткий итог:
«Через 1 день после новости бумага выросла/упала на X%».

Режим «обучения / тренажёра» для инвесторов

Новости показываются без подсказок → пользователь оценивает сам,

После выбора — открывается «оценка нейросети» и реальный исход,

Геймификация: рейтинг, баллы, прогресс.

5. Функции ML-ядра (back-end модели)

ML-модель должна уметь делать несколько вещей одновременно (multi-task):

Извлечение сущностей (NER)

названия компаний, тикеры, индексы,

страны, сектора,

имена регуляторов (ЦБ, ФРС, ЕЦБ и т. д.).

Классификация типа события

категория новости (санкции, отчёт, ставка, M&A, дивиденды и пр.).

Оценка сентимента и направленного эффекта

конкретно для затронутых активов, а не просто «позитив/негатив».

Прогноз «сильности» триггера

вероятность того, что в течение окна Δt (например, 1 день/3 дня)
будет значимое изменение цены > X%.

Формирование краткого резюме

нормальный «human-readable» вывод:

«Новость ухудшает ожидания по прибыли компании X»,

«Вероятно краткосрочное давление на акции банков».

6. Источники данных и обучение модели
6.1. Данные для обучения

Исторические новости + котировки

Текст новости,

Время публикации,

Тикеры / активы, которых касается,

Цены до и после события (через 1ч, 4ч, 1д, 3д).

Правила для авторазметки триггеров (weak supervision)

Если после новости за 1–3 дня актив двинулся > N% относительно рынка →
помечаем как «сильный триггер».

Если движение маленькое → «слабый/нулевой триггер».

Ручная разметка (для повышения качества)

Ты или аналитики размечают часть новостей:

тип события,

направление влияния,

субъективную силу впечатления.

6.2. Структура задачи ML

Модель на вход получает:
текст новости + (опционально) дополнительные признаки (время, эмитент)

На выход:

TriggerScore ∈ [0, 1] (или 0–100),

направление (bullish, bearish, neutral),

тип события (event_type),

список связных активов.

7. Техническая архитектура сервиса (в связке с Miniapp)
7.1. Общая схема

Парсер / аггрегатор новостей

Отдельный сервис, который собирает новости (RSS, API, парсинг сайтов).

Сохраняет в базу: текст, заголовок, источник, время.

ML-сервис (инференс)

REST API, например на FastAPI / Node.js,

Эндпойнты:

POST /analyze_text — анализ произвольного текста,

POST /analyze_url — парсинг + анализ по ссылке,

GET /triggers_feed — список последних триггеров,

GET /triggers_by_ticker — фильтр по тикеру/сектору.

БД

Таблица news — сырые новости,

Таблица triggers — результаты анализа,

Таблица user_subscriptions — настройки подписок Miniapp-пользователей,

Таблица alerts_log — отправленные уведомления.

Telegram Bot + Miniapp

Miniapp как фронтенд (React / Vue), работает в вебвью,

Общение с твоим backend через https API,

Бот отвечает за:

старт,

авторизацию пользователя,

отправку уведомлений.

8. Функциональные блоки в Miniapp (по экранам)

Экран 1 — «Быстрый анализ новости»

Поле: «Вставьте ссылку / текст новости»

Кнопка: «Анализировать»

Результат (карточка):

Заголовок,

Оценка триггера (бар/прогресс),

Направление и тип события,

Затронутые активы,

Краткое объяснение.

Экран 2 — «Лента триггеров»

Список свежих новостей, отсортированный по TriggerScore,

Фильтры: тикеры, сектора, направление, тип события.

Экран 3 — «Подписки / уведомления»

Настройка:

интересующие активы,

минимальный TriggerScore,

тип события (санкции, дивиденды и т.д.).

Визуализация:
«Вы будете получать уведомления, если…»

Экран 4 — «История и точность»

Лента прошлых триггеров с реальным исходом,

Простейшие графики:

сколько сигналов оправдалось,

hit-rate.

Экран 5 — «Обучение / тренажёр» (pro-функция)

Пользователю показывается новость, он оценивает как триггер/не триггер,

Потом видит:

оценку модели,

реальное движение цены.

Геймификация: баллы, streak.

9. Монетизация проекта

Freemium

Бесплатно:

анализ по тексту/ссылке в день с лимитом,

basic лента триггеров.

Платно:

расширенные лимиты,

подписки на уведомления по триггерам,

доступ к истории и статистике.

B2B-лицензии

API-доступ к ML-сервису для:

брокеров,

инвест-каналов с большой аудиторией,

банков.

Брендированные решения

White-label версия Miniapp под бренд брокера/банка,

Интеграция в их экосистему.

10. Что можно сделать следующим шагом

Я могу дальше:

расписать детальный план MVP на 1–2 месяца (конкретные спринты: парсинг → простая модель → API → Miniapp),

предложить стек технологий (Python/Node, БД, библиотеки для NLP),

набросать структуру API (с примерами JSON-ответов),

или помочь с архитектурой данных для обучения модели (схема таблиц, пайплайн сбора и разметки).

План по спринтам (MVP на 4 спринта по 1 неделе)
🟢 Спринт 1 — База: архитектура, backend-каркас, Miniapp-скелет

Цель:
Есть живой backend + простейший Miniapp, который шлёт запрос и получает заглушку.

Задачи:

Инфраструктура

Создать репозиторий (frontend + backend).

Базовая структура проекта:

backend/app/main.py, routers, models, schemas.

frontend/src с React + TS.

Backend: FastAPI-каркас

Поднять FastAPI-приложение.

Добавить эндпойнт POST /analyze с заглушкой:

Принимает text, telegram_id.

Возвращает фиксированный ответ (например, random score + neutral).

База данных

Поднять PostgreSQL (локально / Docker).

Создать таблицы users, news, analysis_results.

Подключить ORM.

В POST /analyze:

Создавать/находить пользователя по telegram_id,

Создавать запись news,

Создавать запись analysis_results (пока с заглушками).

Miniapp: первая версия

Экран с:

полем textarea для текста новости,

кнопкой «Анализировать».

Отправка запроса на backend.

Отображение JSON-результата в простом виде.

Бот

Создать бота в BotFather.

Реализовать /start, который:

показывает кнопку «Открыть приложение» (web_app).

Настроить открытие Miniapp с initData.

🟡 Спринт 2 — Базовая ML-модель (rule-based + простая BERT-модель)

Цель:
Сделать минимально адекватный анализ новости, который уже не стыдно показать людям.

Задачи:

Сбор стартового датасета

Взять немного реальных новостей (можно с открытых источников).

Для MVP: 200–500 новостей.

Ручками проставить:

direction (плюс/минус/нейтрально),

event_type,

приблизительный trigger_score (10/30/60/90).

Baseline-модель (классификация и регрессия)

Взять предобученный ruBERT / multilingual BERT.

Обучить:

классификатор direction (3 класса),

классификатор event_type (несколько классов),

регрессию trigger_score (0–100 условно).

Правила поверх модели

Дополнительно ввести rule-based логики:

если упоминается «санкции», «ограничения», «арест активов» → повышаем trigger_score,

если фраза типа «уменьшил прогноз», «cut guidance» → негатив,

если «увеличил дивиденды», «рекордная прибыль» → позитив.

Интеграция в backend

Обернуть модель в сервис:

при запросе POST /analyze:

прогнать текст через ML,

получить direction, trigger_score, event_type.

Добавить примитивный summary:

генерировать по шаблонам (пока без LLM):
«Новость относится к категории {event_type} и скорее {direction} для {assets} с силой триггера {score}».

Минимальная оптимизация

Сделать так, чтобы обработка одной новости занимала < 1 сек.
(для MVP можно держать модель в памяти в одном процессе).

▶ Статус: реализован гибридный анализатор (`backend/app/ml`) с ruBERT-сентиментом, rule-based поправками, пайплайном загрузки/тренировки (`backend/scripts/train_baseline.py`, структура `data/`). Эндпойнт `/api/analyze` использует новый сервис и сохраняет полные результаты.

Дополнительно:
- Сентимент-модель задаётся переменной `SENTIMENT_MODEL_ID` (по умолчанию `blanchefort/rubert-base-cased-sentiment`).
- Для приватных моделей можно указать `HUGGINGFACE_TOKEN`.
- Базовый sklearn-бандл подключается через `BASELINE_MODEL_PATH`.
- Авторизация Telegram Miniapp включается при наличии `TELEGRAM_BOT_TOKEN`; окно валидности задаётся `TELEGRAM_INIT_MAX_AGE_SECONDS`, дневной лимит — `TELEGRAM_DAILY_REQUEST_CAP` (в рамках окна `TELEGRAM_REQUEST_WINDOW_SECONDS`).

🟠 Спринт 3 — Улучшение UX Miniapp + история запросов

Цель:
Сделать из «демки для себя» нормальный пользовательский продукт.

Задачи:

UI/UX Miniapp

Красивый экран результата:

прогресс-бар/круг с trigger_score,

плашка: «Скорее негативно/позитивно/нейтрально»,

текстовый summary,

тип события (иконки / теги).

Обработка ошибок (нет связи с backend и т.п.).

История анализов

Эндпойнт GET /history?telegram_id=...&limit=20.

В Miniapp:

вкладка «История».

Список карточек предыдущих анализов:

дата,

усечённый заголовок/начало текста,

цвет по направлению,

клик по карточке → подробности.

▶ Статус: реализован эндпойнт `/api/history`, который возвращает последние запросы пользователя. Во фронтенде добавлена вкладка «История» с карточками, кнопкой «Обновить» и защитой от отсутствия Telegram ID. Карточки подсвечиваются по направлению, добавлен детальный просмотр в модалке и подтягивание summary/активов.

Авторизация пользователя

Backend:

принимать initData из Miniapp,

валидировать подпись Telegram,

создавать/идентифицировать user по telegram_id.

▶ Статус: backend валидирует `initData` с использованием `TELEGRAM_BOT_TOKEN`, сопоставляет telegram_id и ограничивает число запросов на пользователя (daily cap). Frontend передаёт initData при анализе и загрузке истории.

Frontend:

получать данные пользователя через window.Telegram.WebApp.initDataUnsafe.

Улучшения ML

Добавить извлечение примитивных related_assets:

словарь: тикер → список ключевых слов / названий компании.

По тексту новости — искать упоминания.

🔴 Спринт 4 — Отладка качества, метрики, подготовка к расширению

Цель:
Сделать MVP устойчивым + заложить основу под масштабирование (уведомления, автоматический парсинг).

Задачи:

Логирование и метрики

Логировать запросы:

длина текста,

время ответа модели,

тип события.

Ввести простую метрику:

% новостей по каждому классу,

сколько раз пользователи вообще пользуются.

Интерфейс обратной связи

В Miniapp:

в карточке результата маленькие кнопки:

«Триггер оценён верно» / «Не согласен».

Backend:

сохранять метки от пользователя в отдельную таблицу feedback.

В будущем это можно использовать для дообучения модели.

▶ Статус: добавлен API `POST /api/feedback`, таблица `analysis_feedback` (migration `c3d2f9f4cfec`) и две кнопки в Miniapp. Пользователь авторизуется через Telegram initData, а ответ сохраняется/обновляется; команда для обновления схемы:

```bash
cd backend
alembic upgrade head
```

После миграции можно собрать фронтенд (`npm run build` в `frontend/`) — в карточке появится блок «Как оцените предсказание?», который пишет фидбек и подсвечивает выбранный вариант.

Техподготовка под следующие фичи

Придумать и описать:

как будет выглядеть таблица subscriptions,

как хранить тикеры / фильтры.

Но не реализовывать прямо сейчас, только схема.

Небольшой рефакторинг ML

Вынести inference в отдельный модуль/класс.

Продумать возможность запуска в Docker-контейнере.

📌 Минимальный функционал, который должен быть готов после MVP

Telegram-бот:

/start → кнопка «Открыть Miniapp».

Miniapp:

Ввод текста новости,

Получение анализа,

Красивый экран с:

TriggerScore,

direction,

event type,

summary,

Вкладка «История».

Backend + ML:

Валидация initData от Telegram,

Сохранение пользователей и анализов,

Рабочая ML-модель, которая:

не стыдная,

более-менее разумно различает «жёсткий негатив», «позитив», «фон».

## 11. Data Foundation (Sprint 0)

1. **Raw история / Feature store конфигурация.**  
	- `config/universe_core.json` задаёт core-тикеры, `config/feature_store.yaml` — описание feature_sets/timeframes (см. `docs/feature_store_v1.md`).  
	- `tech_v2` расширяет `tech_v1` за счёт Bollinger Bands и Stochastic Oscillator; window_size и состав признаков целиком читаются из YAML.  
	- CLI `python -m backend.scripts.ingestion.backfill_candles --start-date YYYY-MM-DD --end-date YYYY-MM-DD --secids SBER GAZP ... --timeframes 1m 5m 15m 1h 1d --chunk-days 14` заполняет `candles` и складывает CSV/parquet в `data/raw/candles/{SECID}/{timeframe}/`.  
	- Prefect flow `candles-ingest-flow` (описан в `backend/scripts/scheduler/prefect_flows.py`) принимает `secids`, `timeframes`, `since/until`, `export_dir`, `min_ratio`, `fail_on_low`.

2. **Prefect пайплайны features/labels.**  
	- Flow `candles-and-features-flow` теперь содержит задачи: ingestion → окна (`build_feature_windows_task`) → экспорт снапшотов (`export_feature_snapshots_task`) → label-инг.  
	- Регистрация деплоймента:  
		```
		python -m backend.scripts.scheduler.register_deployments \
		  --flow-name candles-and-features-flow \
		  --secids SBER GAZP LKOH GMKN \
		  --window-days 2 \
		  --snapshot-output-dir data/processed/features \
		  --apply
		```
	- CLI поддерживает `--disable-snapshot-export`, `--snapshot-file-format feather`, `--snapshot-daily-partition false`.

3. **Экспорт feature snapshots.**  
	- Ручной экспорт: `python -m backend.scripts.features.export_snapshots --secids SBER GAZP --timeframe 1m --feature-set tech_v1 --start-date 2024-10-01T00:00:00+00:00 --end-date 2024-11-15T23:59:00+00:00 --output-dir data/processed/features --format parquet`.  
	- Файлы складываются в `data/processed/features/{feature_set}/{timeframe}/YYYYMMDD.parquet` (или `snapshot_<start>_<end>.parquet`, если отключить daily partition).  
	- Каждый экспорт регистрируется в `train_data_snapshots` (secid/timeframe/feature_set/rows), что позволяет проследить, какой объём попал в обучение.

4. **Контроль качества данных.**  
	- Свечи: `python -m backend.scripts.monitoring.data_quality_report --timeframe 1m --start-date 2024-10-01 --end-date 2024-11-15` → `docs/data_quality/candles_1m_2024-10-01_2024-11-15.json`.  
	- Лейблы: `python -m backend.scripts.labels.build_labels ... --summary-json docs/data_quality/labels_basic_v1_1m_2024-10-01_2024-11-15.json --dry-run` даёт coverage/class balance без записи в БД.  
	- Ноутбук `notebooks/data_quality.ipynb` загружает отчёты и строит heatmap по дням.
	- Дополнительно: `python -m backend.scripts.monitoring.labels_eda --label-set intraday_v1 --timeframe 1m ... --output docs/data_quality/eda_intraday_v1_1m.json` и `... --label-set swing_v1 --timeframe 1h ... --output docs/data_quality/eda_swing_v1_1h.json` фиксируют coverage/expectancy по каждому горизонту.
	- Для снапшотов Feature Store используем `python -m backend.scripts.monitoring.snapshot_report --feature-set tech_v2 --timeframe 1h --since 2024-10-01T00:00:00+00:00 --output docs/data_quality/train_snapshots_tech_v2_1h.json`.

5. **Документация и чек-лист.**  
	- `docs/feature_store_v1.md` описывает архитектуру Feature Store, `config/label_sets.yaml` — пресеты label_set (basic_v1, intraday_v1, swing_v1).  
	- Этот раздел и `ML.md` обновлены: фиксируем изменения, даты и ссылки на коммиты.

## 12. ML Infra & Training Flow

- **Flow `train-temporal-model-flow`.**  
	1. Готовит baseline-датасет (`prepare_baseline_dataset_task`, параметры из `config/feature_store.yaml`/`config/label_sets.yaml`).  
	2. Запускает `train_temporal_cnn.py` с нужными гиперпараметрами и (опционально) MLflow.  
	3. Регистрируется через  
		```powershell
		python -m backend.scripts.scheduler.register_deployments `
		  --flow-name train-temporal-model-flow `
		  --secids SBER GAZP `
		  --train-feature-set tech_v1 `
		  --train-label-set basic_v1 `
		  --train-start-date 2024-10-01 `
		  --train-end-date 2024-11-15 `
		  --train-mlflow-tracking-uri http://localhost:5000 `
		  --train-mlflow-experiment TemporalCNN `
		  --apply
		```

		> ⚠️ **Windows + Prefect CLI:** перед любыми `prefect ...` командами выполняйте `chcp 65001`, чтобы консоль не падала на Unicode-бокс-символах (CP1251 не поддерживает их вывод). Добавьте `chcp 65001` в профиль PowerShell, если часто запускаете деплойменты вручную.
- **MLflow поддержка.**  
	`backend/scripts/training/train_temporal_cnn.py` принимает `--mlflow-tracking-uri`, `--mlflow-experiment`, `--mlflow-run-name`, `--mlflow-tags key=value`. При указании URI автоматически логируются параметры, метрики (ROC-AUC, PR-AUC, accuracy и т.д.) и артефакты (`logs/temporal_cnn`).  
- **Dry-run лейблов.**  
	`backend/scripts/labels/build_labels.py` умеет `--dry-run` для генерации отчётов без записи в DB — удобно для Prefect-валидаторов и отчётов в `docs/data_quality/`.


> **Новое:** Flow 	rain-temporal-model-flow поддерживает grid-search и уведомления. Передавайте --train-grid-json path/to/grid.json (список словарей с гиперпараметрами) и --train-notification-url https://hooks.slack.com/..., чтобы запускать несколько тренировок и получать сообщение с метриками (ROC/PR/accuracy) после каждой. ackend/scripts/training/train_temporal_cnn.py логирует метрики в MLflow (если указан --train-mlflow-tracking-uri) и сохраняет TensorBoard/CSV + ROC-кривые в logs/temporal_cnn.


## 13. Temporal Model Inference

- ackend/app/ml/temporal_inference.py содержит сервис для загрузки чекпоинта Temporal CNN. Установите переменные TEMPORAL_MODEL_CHECKPOINT, TEMPORAL_FEATURE_COLUMNS (через запятую) и TEMPORAL_SEQ_LEN, после чего используйте ackend/app/ml/service.py:get_temporal_model_service() внутри API.
- Пример: TEMPORAL_MODEL_CHECKPOINT=logs/temporal_cnn/checkpoints/temporal-cnn-epoch.ckpt и TEMPORAL_FEATURE_COLUMNS=return_1,sma_ratio_5_20,...,news_count_60m.
- Сервис возвращает вероятность long-сигнала и может быть вызван из FastAPI эндпойнта /api/analyze_ticker.
