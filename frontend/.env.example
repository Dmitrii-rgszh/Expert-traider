# Переопределяет базовый URL API. Если не задано, фронтенд использует http://localhost:8000/api локально
# и автоматически переключается на "<текущий-origin>/api" при работе через туннель.
VITE_API_URL=http://localhost:8000/api
